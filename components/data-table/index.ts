/**
 * components/data-table — Headless TanStack Table powered data table.
 * Wraps the existing DataTable with the TanStack ecosystem.
 *
 * Usage: import { DataTable } from '@/components/data-table'
 */
export { DataTable } from '@/components/DataTable';
